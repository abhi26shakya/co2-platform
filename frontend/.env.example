BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=Emissia
# Mapbox access token (client-exposed by design — Mapbox tokens are meant to be public and are
# restricted via URL/referrer allow-listing in the Mapbox account dashboard, not by secrecy).
# Get one at https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=
